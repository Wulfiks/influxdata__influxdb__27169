[
  {
    "instance_id": "73b49642-1124-4634-b2a9-f2e486302409",
    "task_title": "73b49642-1124-4634-b2a9-f2e486302409",
    "problem_statement": "InfluxDB's TSM compaction planner reads the point count of each candidate file's opening block on every planning cycle. The hot read goes through FileStore.BlockCount(path, 1), which on each call walks the entire file list to find the matching path, opens a fresh block iterator on the matching reader, decodes the opening block's header just to read the point count, and discards the iterator. On a shard with thousands of TSM files the planner spends most of its CPU in this loop, and the cost recurs every time the planner runs.\n \n\n Cache the opening block's point count for each file alongside the rest of its summary stats. Add an ExtFileStat struct that embeds FileStat and carries a FirstBlockCount int. Add an ExtStats() (ExtFileStat, error) method to TSMReader that computes the opening block's count once and memoizes the result on the reader so subsequent calls are cheap. Add a sibling method ExtStats() []ExtFileStat to FileStore that returns one ExtFileStat per file, populated by asking each file for its extended stats, and caches the slice so repeated planner reads do not rebuild it.\n \n\n The compaction planner in compact.go should switch its callers of FileStore.BlockCount(path, 1) over to reading FirstBlockCount from the cached ExtFileStat slice. ExtFileStat embeds FileStat so existing field accesses like Path, Size, MinTime, MaxTime, and HasTombstone continue to resolve on each entry of the returned slice without change.",
    "problem_statement_variant": "InfluxDB's TSM compaction planner spends most of its CPU decoding the opening block of each candidate file on every planning cycle. Each call to the file store's per file opening block reader allocates a block iterator and decodes the header just to read one integer, and the planner loops over thousands of files. Cache the point count of each file's opening block alongside the rest of its summary stats so the planner reads it directly from the cached slice. The cache should be lazy on the reader, computed on first read and returned thereafter, and rebuilt on the file store whenever the file set changes.",
    "hints": "Three files in `tsdb/engine/tsm1/`. In `tsdb/engine/tsm1/file_store.go`, declare the new `ExtFileStat` struct that embeds `FileStat` with a `FirstBlockCount int` field. Add a small `ToExtFileStat()` helper on `FileStat`. Add a new method `FileStore.ExtStats() []ExtFileStat` that mirrors the cached and locked pattern of the existing `Stats()` method, with a typed fallback for mocks that only implement the historical interface. In `tsdb/engine/tsm1/reader.go`, add a small cache field on `TSMReader` plus an `ExtStats() (ExtFileStat, error)` method that calls a new private helper which decodes block index 1 once and caches the result under the reader's mutex. In `tsdb/engine/tsm1/compact.go`, change `tsmGeneration.files` to `[]ExtFileStat`. Have `findGenerations` source its stats from a small helper that prefers `FileStore.ExtStats()` and falls back to building `ExtFileStat` values from the legacy `Stats()` plus `BlockCount(path, 1)` for test mocks. Switch the four `c.FileStore.BlockCount(group.files[0].Path, 1)` callsites in the planner over to `tsmFile.FirstBlockCount` reads.\n \n\n Order of changes: add the new struct, helper, and capability interface in `file_store.go`, then the new method on `*TSMReader`, then add `FileStore.ExtStats()` using the capability interface, then thread the new slice through the planner. Tests validate that `FileStore.ExtStats()` returns one entry per TSM file with the opening block's point count populated to the actual number of points in the file's first block.",
    "repo": "influxdb",
    "repo_path_or_url": "https://github.com/handshake-coding/influxdata__influxdb__27169",
    "FAIL_TO_PASS": "[\"tsdb/engine/tsm1/ext_file_stat_test.go\",\"tsdb/engine/tsm1/file_store.go\"]",
    "PASS_TO_PASS": "[\"tsdb/engine/tsm1/compact_test.go\",\"tsdb/engine/tsm1/file_store_test.go\"]",
    "language": "Go",
    "docker_file": "FROM golang:1.24.0\n \n\n SHELL [\"/bin/bash\", \"-o\", \"pipefail\", \"-c\"]\n \n\n WORKDIR /app\n \n\n RUN apt-get update && apt-get install -y --no-install-recommends git ca-certificates curl wget jq ripgrep build-essential pkg-config clang xz-utils libsqlite3-dev && rm -rf /var/lib/apt/lists/*\n \n\n RUN check_min() {  tool=$1 ; min=$2 ; actual=$3 ; ok=$(awk \"BEGIN{ split(\\\"$actual\\\",a,\\\".\\\") ; split(\\\"$min\\\",m,\\\".\\\") ; for(i=1 ; i<=3 ; i++){ if(a[i]+0>m[i]+0){print 1 ; exit} if(a[i]+0<m[i]+0){print 0 ; exit} } ; print 1}\") ; if [ \"$ok\" -ne 1 ] ; then  echo \"FAIL: $tool $actual < minimum $min\" >&2 ; exit 1 ; fi ; echo \"OK: $tool $actual >= $min\" ; } ; check_min git 2.25.0 \"$(git --version | awk '{print $3}')\" && check_min curl 7.70.0 \"$(curl --version | head -1 | awk '{print $2}')\" && check_min jq 1.6 \"$(jq --version | sed 's/^jq-//')\" && check_min rg 13.0.0 \"$(rg --version | head -1 | awk '{print $2}')\"\n \n\n RUN check_min() {  tool=$1 ; min=$2 ; actual=$3 ; ok=$(awk \"BEGIN{ split(\\\"$actual\\\",a,\\\".\\\") ; split(\\\"$min\\\",m,\\\".\\\") ; for(i=1 ; i<=3 ; i++){ if(a[i]+0>m[i]+0){print 1 ; exit} if(a[i]+0<m[i]+0){print 0 ; exit} } ; print 1}\") ; if [ \"$ok\" -ne 1 ] ; then  echo \"FAIL: $tool $actual < minimum $min\" >&2 ; exit 1 ; fi ; echo \"OK: $tool $actual >= $min\" ; } ; check_min wget 1.20.0 \"$(dpkg-query -W -f='${Version}' wget | sed 's/^[0-9]*:// ; s/-.*// ; s/+.*//')\"\n \n\n ENV RUSTUP_HOME=/usr/local/rustup CARGO_HOME=/usr/local/cargo PATH=/usr/local/cargo/bin:$PATH\n \n\n RUN curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --default-toolchain 1.94 --profile minimal && rustup component add rustfmt clippy\n \n\n COPY . .\n \n\n ENV CGO_ENABLED=1\n \n\n RUN go mod download && go build -o /usr/local/bin/pkg-config github.com/influxdata/pkg-config\n \n\n ENV PKG_CONFIG=/usr/local/bin/pkg-config\n \n\n CMD [\"sh\", \"-c\", \"go test ./...\"]",
    "run_script": "#!/usr/bin/env bash\n set -euo pipefail\n \n\n SCRIPT_DIR=\"$(cd \"$(dirname \"${BASH_SOURCE[0]}\")\" && pwd)\"\n REPO_ROOT=\"$(cd \"$SCRIPT_DIR/..\" && pwd)\"\n cd \"$REPO_ROOT\"\n \n\n DOCKERFILE=\"$SCRIPT_DIR/Dockerfile.helix\"\n \n\n die() { echo \"Error: $*\" >&2; exit 1; }\n info() { echo \"$*\" >&2; }\n \n\n # \u2500\u2500 Parse test command from Dockerfile.helix (CMD or ENTRYPOINT) \u2500\u2500\n \n\n parse_instruction() {\n  local raw=\"$1\"\n  if [[ \"$raw\" == \\[* ]]; then\n  local inner=\"${raw#\\[}\"\n  inner=\"${inner%\\]}\"\n  local result=\"\" elem=\"\"\n  while IFS= read -r -d ',' elem || [[ -n \"$elem\" ]]; do\n  elem=$(echo \"$elem\" | sed 's/^[[:space:]]*\"//; s/\"[[:space:]]*$//')\n  [[ -z \"$elem\" ]] && continue\n  if [[ \"$elem\" == *\" \"* ]]; then\n  elem=\"${elem//\\\\/\\\\\\\\}\"\n  elem=\"${elem//\\\"/\\\\\\\"}\"\n  result+=\"\\\"${elem}\\\" \"\n  else\n  result+=\"$elem \"\n  fi\n  done <<< \"$inner\"\n  echo \"$result\" | sed 's/ *$//'\n  else\n  echo \"$raw\"\n  fi\n }\n \n\n extract_cmd() {\n  local cmd_line=\"\" ep_line=\"\"\n  while IFS= read -r line || [[ -n \"$line\" ]]; do\n  local trimmed=\"${line#\"${line%%[![:space:]]*}\"}\"\n  case \"${trimmed}\" in\n  CMD\\ *|cmd\\ *)\n  cmd_line=\"${trimmed:4}\"; cmd_line=\"${cmd_line#\"${cmd_line%%[![:space:]]*}\"}\" ;;\n  ENTRYPOINT\\ *|entrypoint\\ *)\n  ep_line=\"${trimmed:11}\"; ep_line=\"${ep_line#\"${ep_line%%[![:space:]]*}\"}\" ;;\n  esac\n  done < \"$DOCKERFILE\"\n \n\n  if [[ -n \"$cmd_line\" ]]; then\n  local cmd; cmd=$(parse_instruction \"$cmd_line\")\n  if [[ -n \"$ep_line\" ]]; then\n  local ep; ep=$(parse_instruction \"$ep_line\")\n  echo \"${ep} ${cmd}\"\n  else\n  echo \"$cmd\"\n  fi\n  elif [[ -n \"$ep_line\" ]]; then\n  parse_instruction \"$ep_line\"\n  else\n  die \"No CMD or ENTRYPOINT found in $DOCKERFILE\"\n  fi\n }\n \n\n # \u2500\u2500 Resolve yarn/npm/pnpm script aliases \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n \n\n resolve_script() {\n  local cmd=\"$1\" script_name=\"\"\n \n\n  if [[ \"$cmd\" =~ ^(yarn|pnpm)[[:space:]]+([a-zA-Z][^[:space:]]*) ]]; then\n  script_name=\"${BASH_REMATCH[2]}\"\n  elif [[ \"$cmd\" =~ ^(npm|pnpm)[[:space:]]+run[[:space:]]+([^[:space:]]+) ]]; then\n  script_name=\"${BASH_REMATCH[2]}\"\n  elif [[ \"$cmd\" =~ ^npm[[:space:]]+test ]]; then\n  script_name=\"test\"\n  fi\n \n\n  if [[ -z \"$script_name\" ]]; then echo \"$cmd\"; return; fi\n  if [[ ! -f \"package.json\" ]]; then echo \"$cmd\"; return; fi\n \n\n  local resolved=\"\"\n  if command -v node &>/dev/null; then\n  resolved=$(node -e \"\n  try { console.log(require('./package.json').scripts['$script_name'] || '') }\n  catch(e) { console.log('') }\n  \" 2>/dev/null) || true\n  elif command -v python3 &>/dev/null; then\n  resolved=$(python3 -c \"\n import json\n print(json.load(open('package.json')).get('scripts',{}).get('$script_name',''))\n \" 2>/dev/null) || true\n  fi\n \n\n  [[ -n \"$resolved\" ]] && echo \"$resolved\" || echo \"$cmd\"\n }\n \n\n # \u2500\u2500 Detect the underlying test runner \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n \n\n detect_runner() {\n  local cmd=\"$1\"\n  case \"$cmd\" in\n  pytest*|*\" pytest \"*|python*-m*pytest*)  echo \"pytest\" ;;\n  *vitest*) echo \"vitest\" ;;\n  *jest*) echo \"jest\" ;;\n  *mocha*)  echo \"mocha\" ;;\n  go\\ test*)  echo \"go\" ;;\n  cargo\\ test*|cargo\\ nextest*) echo \"cargo\" ;;\n  mvn\\ *|./mvnw\\ *)  echo \"maven\" ;;\n  *gradle*test*|*gradlew*test*) echo \"gradle\" ;;\n  *rspec*)  echo \"rspec\" ;;\n  *phpunit*)  echo \"phpunit\" ;;\n  dotnet\\ test*)  echo \"dotnet\" ;;\n  mix\\ test*) echo \"mix\" ;;\n  swift\\ test*) echo \"swift\" ;;\n  sbt*test*)  echo \"sbt\" ;;\n  *nose2*)  echo \"nose2\" ;;\n  ruby*-I*|ruby*test*|rake\\ test*)  echo \"ruby\" ;;\n  *ctest*|*\" ctest \"*)  echo \"ctest\" ;;\n  meson\\ test*|*\"meson test\"*)  echo \"meson\" ;;\n  *)  echo \"unknown\" ;;\n  esac\n }\n \n\n # \u2500\u2500 Strip coverage flags (prevent coverage gates from failing eval) \u2500\u2500\n \n\n strip_coverage_flags() {\n  local runner=\"$1\" cmd=\"$2\"\n  case \"$runner\" in\n  pytest)  cmd=$(echo \"$cmd\" | sed -E 's/ --cov[=-][^ ]*//g; s/ --cov\\b//g; s/ --no-cov-on-fail//g') ;;\n  jest|vitest) cmd=$(echo \"$cmd\" | sed -E 's/ --coverage[^ ]*//g') ;;\n  go)  cmd=$(echo \"$cmd\" | sed -E 's/ -cover\\b//g; s/ -coverprofile[= ][^ ]*//g; s/ -coverpkg[= ][^ ]*//g') ;;\n  phpunit) cmd=$(echo \"$cmd\" | sed -E 's/ --coverage-[^ ]*//g') ;;\n  mix) cmd=$(echo \"$cmd\" | sed -E 's/ --cover\\b//g') ;;\n  dotnet)  cmd=$(echo \"$cmd\" | sed -E 's/ --collect:[^ ]*//g; s| /p:CollectCoverage=[^ ]*||g; s| /p:Threshold=[^ ]*||g') ;;\n  esac\n  echo \"$cmd\"\n }\n \n\n # \u2500\u2500 JVM: convert file path to fully-qualified class name \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n \n\n path_to_classname() {\n  local f=\"$1\"\n  for prefix in src/test/java/ src/test/kotlin/ src/test/scala/ \\\n  src/main/java/ src/main/kotlin/ src/main/scala/ \\\n  src/it/java/ src/it/kotlin/ src/it/scala/; do\n  f=\"${f#$prefix}\"\n  done\n  for ext in .java .kt .scala .groovy .kts; do\n  f=\"${f%$ext}\"\n  done\n  echo \"${f//\\//.}\"\n }\n \n\n # \u2500\u2500 Construct the test command for a specific set of files \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n \n\n build_targeted_cmd() {\n  local runner=\"$1\" full_cmd=\"$2\"\n  shift 2\n  local files=(\"$@\")\n \n\n  case \"$runner\" in\n  pytest)  echo \"pytest ${files[*]}\" ;;\n  vitest)  echo \"vitest run ${files[*]}\" ;;\n  jest)  echo \"npx jest --watchAll=false ${files[*]}\" ;;\n  mocha) echo \"npx mocha ${files[*]}\" ;;\n  nose2) echo \"nose2 ${files[*]}\" ;;\n  mix) echo \"mix test ${files[*]}\" ;;\n  ruby)  echo \"ruby ${files[*]}\" ;;\n  rspec)\n  if command -v bundle &>/dev/null; then\n  echo \"bundle exec rspec ${files[*]}\"\n  else\n  echo \"rspec ${files[*]}\"\n  fi\n  ;;\n  phpunit)\n  if [[ -f \"./vendor/bin/phpunit\" ]]; then\n  echo \"./vendor/bin/phpunit ${files[*]}\"\n  else\n  echo \"phpunit ${files[*]}\"\n  fi\n  ;;\n  go)\n  declare -A _go_dirs=()\n  for f in \"${files[@]}\"; do\n  _go_dirs[\"./$(dirname \"$f\")\"]=1\n  done\n  echo \"go test ${!_go_dirs[*]}\"\n  ;;\n  cargo)\n  local is_workspace=0\n  if [[ -f \"Cargo.toml\" ]] && grep -q '^\\[workspace\\]' \"Cargo.toml\" 2>/dev/null; then\n  is_workspace=1\n  fi\n \n\n  # Preserve the base command with all its flags (--features, --release, etc.)\n  # by stripping only the trailing \"-- <args>\" portion.\n  local base_cmd\n  base_cmd=\"$(echo \"$full_cmd\" | sed 's/[[:space:]]\\+--[[:space:]]\\+.*$//')\"\n \n\n  declare -A _cargo_seen=()\n  local cmds=()\n  for f in \"${files[@]}\"; do\n  local pkg=\"\" pkg_dir=\"\"\n  if (( is_workspace )); then\n  local d\n  d=\"$(dirname \"$f\")\"\n  while [[ -n \"$d\" && \"$d\" != \".\" ]]; do\n  if [[ -f \"$d/Cargo.toml\" ]] && grep -q '^\\[package\\]' \"$d/Cargo.toml\" 2>/dev/null; then\n  pkg=$(sed -n '/^\\[package\\]/,/^\\[/{s/^name[[:space:]]*=[[:space:]]*\"\\([^\"]*\\)\"/\\1/p;}' \"$d/Cargo.toml\" 2>/dev/null | head -1)\n  pkg_dir=\"$d\"\n  break\n  fi\n  d=\"$(dirname \"$d\")\"\n  done\n  fi\n  local rel=\"$f\"\n  [[ -n \"$pkg_dir\" ]] && rel=\"${f#$pkg_dir/}\"\n  local flag=\"\"\n  if [[ \"$rel\" == tests/* ]]; then\n  flag=\"--test $(basename \"$f\" .rs)\"\n  else\n  flag=\"--lib\"\n  fi\n  local key=\"${pkg:-__root__}::${flag}\"\n  if [[ -z \"${_cargo_seen[$key]:-}\" ]]; then\n  _cargo_seen[\"$key\"]=1\n  local pfx=\"\"\n  [[ -n \"$pkg\" ]] && pfx=\"-p $pkg \"\n  cmds+=(\"${base_cmd} ${pfx}${flag}\")\n  fi\n  done\n  local result=\"\"\n  for ((i=0; i<${#cmds[@]}; i++)); do\n  (( i > 0 )) && result+=\" && \"\n  result+=\"${cmds[$i]}\"\n  done\n  echo \"$result\"\n  ;;\n  maven)\n  local classes=()\n  for f in \"${files[@]}\"; do classes+=(\"$(path_to_classname \"$f\")\"); done\n  local joined; joined=\"$(IFS=,; echo \"${classes[*]}\")\"\n  [[ -f \"./mvnw\" ]] && echo \"./mvnw test -Dtest=${joined}\" || echo \"mvn test -Dtest=${joined}\"\n  ;;\n  gradle)\n  local args=\"\"\n  for f in \"${files[@]}\"; do args+=\" --tests $(path_to_classname \"$f\")\"; done\n  [[ -f \"./gradlew\" ]] && echo \"./gradlew test${args}\" || echo \"gradle test${args}\"\n  ;;\n  dotnet)\n  local parts=()\n  for f in \"${files[@]}\"; do parts+=(\"FullyQualifiedName~$(basename \"$f\" .cs)\"); done\n  local filter; filter=\"$(IFS='|'; echo \"${parts[*]}\")\"\n  echo \"dotnet test --filter \\\"${filter}\\\"\"\n  ;;\n  swift)\n  local names=()\n  for f in \"${files[@]}\"; do names+=(\"$(basename \"$f\" .swift)\"); done\n  local joined; joined=\"$(IFS=,; echo \"${names[*]}\")\"\n  echo \"swift test --filter ${joined}\"\n  ;;\n  sbt)\n  local names=()\n  for f in \"${files[@]}\"; do names+=(\"$(path_to_classname \"$f\")\"); done\n  echo \"sbt 'testOnly ${names[*]}'\"\n  ;;\n  ctest)\n  # Subset by CTest regex (-R): join basenames (strip common source extensions).\n  local rx=\"\" b f\n  for f in \"${files[@]}\"; do\n  b=$(basename \"$f\")\n  b=\"${b%.cpp}\"; b=\"${b%.cc}\"; b=\"${b%.cxx}\"; b=\"${b%.c}\"; b=\"${b%.hpp}\"\n  [[ -n \"$rx\" ]] && rx+=\"|\"\n  rx+=\"$b\"\n  done\n  if [[ \"$full_cmd\" =~ ^[[:space:]]*ctest(.*)$ ]]; then\n  echo \"ctest -R $(printf '%q' \"$rx\")${BASH_REMATCH[1]}\"\n  else\n  echo \"ctest -R $(printf '%q' \"$rx\") --test-dir build --output-on-failure\"\n  fi\n  ;;\n  meson)\n  # Best-effort: meson test accepts test names as positional args (project-defined).\n  local names=()\n  for f in \"${files[@]}\"; do names+=(\"$(basename \"$f\" .cpp)\"); done\n  echo \"meson test -C builddir --print-errorlogs ${names[*]}\"\n  ;;\n  *)\n  info \"Warning: unknown runner \u2014 appending files to original command\"\n  echo \"${full_cmd} ${files[*]}\"\n  ;;\n  esac\n }\n \n\n # \u2500\u2500 Main \u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\n \n\n [[ ! -f \"$DOCKERFILE\" ]] && die \"$DOCKERFILE not found in $(pwd)\"\n \n\n FULL_CMD=$(extract_cmd)\n RESOLVED=$(resolve_script \"$FULL_CMD\")\n RUNNER=$(detect_runner \"$RESOLVED\")\n \n\n if [[ $# -eq 0 || -z \"${1:-}\" ]]; then\n  FULL_CMD=$(strip_coverage_flags \"$RUNNER\" \"$FULL_CMD\")\n  info \"=== Full test suite ===\"\n  info \"Command: $FULL_CMD\"\n  info \"========================\"\n  eval \"$FULL_CMD\"\n else\n  IFS=',' read -ra TEST_FILES <<< \"$1\"\n \n\n  TARGETED=$(build_targeted_cmd \"$RUNNER\" \"$FULL_CMD\" \"${TEST_FILES[@]}\")\n  TARGETED=$(strip_coverage_flags \"$RUNNER\" \"$TARGETED\")\n \n\n  info \"=== Targeted tests ===\"\n  info \"Runner: $RUNNER\"\n  info \"Files:  ${TEST_FILES[*]}\"\n  info \"Command: $TARGETED\"\n  info \"========================\"\n  eval \"$TARGETED\"\n fi",
    "task_type": "hints",
    "task_category": "performance",
    "repo_category": "database",
    "version": "2",
    "container_mem": "4g",
    "container_memswap": "4g",
    "container_network_needed": "TRUE"
  }
]